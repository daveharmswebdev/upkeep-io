// This test is intentionally failing to verify branch protection blocks merges
// Delete this file after verifying protection works

describe('Branch Protection Verification', () => {
  it('should fail intentionally to test branch protection', () => {
    expect(true).toBe(false); // Intentional failure
  });
});
