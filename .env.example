# Root .env file (optional - individual apps have their own .env files)

# Backend environment variables are in apps/backend/.env
# Frontend environment variables are in apps/frontend/.env

# For local development, copy this file to .env and update values as needed
